<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>School Menus</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .menu-card { margin-bottom: 1rem; }
    .menu-date { background-color: #f8f9fa; padding: 0.5rem; border-radius: 0.25rem; }
    .menu-item { padding-left: 1.5rem; }
  </style>
</head>
<body>
<div class="container mt-5">
  <h1 class="mb-4 text-center">School Menus</h1>

  {{#each menus}}
    <div class="mb-5">
      <h2 class="text-capitalize">{{mealType}}</h2>

      {{#each dates}}
        <div class="menu-card">
          <h5 class="menu-date">{{date}}</h5>
          <ul class="list-group list-group-flush">
            {{#each items}}
              <li class="list-group-item menu-item">{{this}}</li>
            {{/each}}
          </ul>
        </div>
      {{/each}}

    </div>
  {{/each}}

</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
